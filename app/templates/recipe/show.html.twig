{% extends 'base.html.twig' %}

{% block title %}{{ title ?? 'Recipe' }}{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            {% include 'partial/recipe/basicInfoCard.html.twig' %}
        </div>

        <div class="col-12">
            <div class="row">
                <div class="card-group">
                    {% include 'partial/recipe/detailsCard.html.twig' %}
                    {% include 'partial/recipe/instructionsCard.html.twig' %}
                </div>
            </div>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-12">
            {% include 'partial/recipe/actionsCard.html.twig' %}
            {% include 'partial/recipe/commentsCard.html.twig' %}
        </div>
    </div>
</div>

{% endblock %}
